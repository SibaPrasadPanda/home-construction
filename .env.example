# Environment variables for NivesaTracker
# Update the values below with your Supabase credentials

DATABASE_URL=postgresql://<user>:<password>@<host>:5432/<db>?sslmode=require
SESSION_SECRET=your-random-session-secret

# (Optional) If you use Vite for the frontend API base URL
VITE_API_URL=http://localhost:5001
